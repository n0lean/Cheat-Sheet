# Hexo Cheat Sheet

```bash
hexo init [folder] # new website
hexo new [layout] <title> # new post
hexo generate # generate static files
hexo g
hexo server # local server
hexo s
hexo deploy # deploy website
hexo d

hexo clean # clean caches and static files
```

